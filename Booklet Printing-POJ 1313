Booklet Printing-POJ 1313;

Kaeya; 

20190927; 

简单模拟;

题目大意：给一个n表示有1~n为有内容的页码，用最少的纸张做成折叠小册，使页码按册子翻页的顺序排好，从最外面的纸张开始-
          输出每张纸上的正面反面的页码。
          
思路：每一面的页码对应一个最小一个最大，一个次大一个次小……以此类推，正面右半页为小页码，背面左半页为小页码，模拟即可。

代码如下：

#include<bits/stdc++.h>

using namespace std;

int n,front,last,page=0;

int main(){
    while(1){
        cin>>n;
        if(n==0)break;

        cout<<"Printing order for "<<n<<" pages:"<<endl;
        front=1;
        if(n%4==0)last=n;
        else if(n==1)last=2;
        else
            last=n+4-n%4;
        page=0;

        for(;;){
            if(page&1){
                cout<<"Sheet "<<page/2+1<<", back : ";
                cout<<front<<", ";
                if(last>n)
                    cout<<"Blank";
                else cout<<last;
                
            }
            else{
                cout<<"Sheet "<<page/2+1<<", front: ";
                if(last>n)
                    cout<<"Blank";
                else cout<<last;
                cout<<", "<<front;
            }
            ++front;
            --last;
            ++page;
            cout<<endl;
            if(last<=front)break;
        }
    }



    return 0;
}
